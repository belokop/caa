# path:
#   The path for this route
# defaults:
#   _content: This is a content page, our class provides a render array
#   _controller: A controller class does all of the processing for this path
#   _title: A static title for our page
#   _title_callback: The page title is generated by a method in our class
# requirements:
#   _permission: only give access to users with this permission
#   _role: only give access to users with this role
#   _access: access is either granted (TRUE) or not (FALSE)
#   _custom_access: access is determined by a method in our class

example.main_destination:
  path: /destination
  requirements:
    _access: 'TRUE'
  defaults:
    _title: M Choose the destination
    _controller:     Drupal\example\ExampleController::getContent

example.destination:
  path: /destination
  defaults:
    _title: Choose the destination
    _controller:     Drupal\example\ExampleController::getContent
  requirements:
    _access: 'TRUE'

example.dest_saturn:
  path: /destination/saturn
  defaults:
    _controller:     Drupal\example\ExampleController::getContent
    _title_callback: Drupal\example\ExampleController::getTitle
  requirements:
# ... not yet tested  
    _custom_access:  Drupal\example\ExampleController::getAccess
# Hides not only the "proper" link, but also all the links below....
#    _permission: 'zzz access example content'
# menu item is shown
#    _access: 'FALSE'   

example.dest_jupiter:
  path: /destination/jupiter
  defaults:
    _controller:     Drupal\example\ExampleController::getContent
    _title_callback: Drupal\example\ExampleController::getTitle
  requirements:
    _custom_access:  Drupal\example\ExampleController::getAccess
